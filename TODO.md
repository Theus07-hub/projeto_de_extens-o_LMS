- [x] Entender o que o projeto usa como “abas” (menu do header `ul.menu`).
- [x] Corrigir CSS base do menu em `public/CSS/pagina.css` (estados, padding, responsivo).
- [x] Garantir que as páginas de curso carreguem `public/CSS/pagina.css` junto do CSS específico do curso.
- [ ] Padronizar `pagina_medicina.html` para usar a mesma estrutura das outras páginas (header/nav/main/footer + body classes).
- [ ] Incluir `pagina.css` e `medicina.css` de forma consistente e aplicar fundo/cores por curso.
- [ ] Ajustar links do menu/voltar para funcionarem com o mesmo prefixo (/public/HTML/...) ou sem ele (definir padrão).
- [ ] Revisar `pagina_ADS.html` (já carrega `ads.css`) para garantir header/menu consistente.
- [ ] Testar abrindo cada `pagina_*.html` e conferindo se o CSS do menu e cores funcionam.

